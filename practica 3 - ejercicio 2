#include <iostream>
using namespace std;
int main() {
    int N;
    int suma = 0;
    cout << "Ingrese N: ";
    cin >> N;
    for (int i = 1; i <= N; i += 2) {
        suma = suma + i;
    }
    cout << "La suma de impares es: " << suma << endl;
    return 0;
}
