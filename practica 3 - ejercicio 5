#include <iostream>
using namespace std;
int main() {
    int N;
    long long producto = 1;
    cout << "Ingrese N: ";
    cin >> N;
    for (int i = 1; i <= N; i++) {
        producto = producto * i;
    }
    cout << "El producto es: " << producto << endl;
    return 0;
}
