#include <stdio.h>

int main(){
    float numero1, numero2;

    printf("ingrese el primer numero: ");
    scanf("%f", &numero1);

    printf("ingrese el segundo numero: ");
    scanf("%f", &numero2);

    if(numero1 > numero2){
        printf("es mayor: %.2f\n", numero1);
    } else {
        printf("es mayor: %.2f\n", numero2);
    }

    return 0;
}
